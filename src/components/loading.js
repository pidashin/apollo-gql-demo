import React from 'react'
import styles from './loading.module.scss'

import { ReactComponent as Logo } from '../../public/logo.svg'

const Loading = () => <Logo className={styles.loading} />

export default Loading
